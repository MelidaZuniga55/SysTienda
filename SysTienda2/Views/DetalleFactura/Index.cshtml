@model IEnumerable<SysTienda2.Models.DetalleFactura>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdFactura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdDetalleFacturaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Servicio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdFactura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDetalleFacturaNavigation.IdFactura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Servicio.IdServicio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDetalleFactura">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDetalleFactura">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDetalleFactura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
